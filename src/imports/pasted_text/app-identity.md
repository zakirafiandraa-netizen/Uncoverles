Overall App Identity

The app uses a teal/green primary color, clean white cards, and a medical stethoscope icon as the logo. Font is modern sans-serif. The vibe is friendly but clinical.

Screen 1 — Home Screen (Uncoverles)

Three mode cards: Offline Mode, Online Mode, and Guidebook. Each has an icon, title, subtitle, and a CTA button. The Online Mode card has an "Online" badge. A tagline sits below the logo.
Figma prompt: "Mobile home screen with app logo centered at top, subtitle below, then 3 equal-width cards in a row. Each card has an icon, title, short description, 2 stat lines, and a colored CTA button. Middle card has a badge. Light background, teal accent."

Screen 2 — Offline Mode Setup (Step 1: Add Players)

Stepper navigation at top (3 steps). Input field for player name, avatar row with selectable icons, player list below with role distribution tags (Civilian, Undercover, Mr. White) at the bottom.
Figma prompt: "Mobile setup flow screen. Top progress stepper with 3 steps. Name input with Add button, horizontal avatar icon selector, player list with avatar chips, role distribution tag row at bottom, Back and Next buttons."

Screen 3 — Offline Mode Setup (Step 2: Disease Category)

Grid of category cards (Acak/Random, Penyakit Jantung, Paru-paru, etc.). Simple tappable tiles with labels.
Figma prompt: "Category selection screen with section title and subtitle. 3-column grid of rounded rectangle tiles, each with a label. First tile is 'Random'. Back and Next navigation buttons."

Screen 4 — Offline Mode Setup (Step 3: Game Summary)

Summary card showing player count, category, role distribution with icons and counts (Civilian/Undercover/Mr. White), and a full player list before starting.
Figma prompt: "Game summary screen. Two-column info row (player count + category). Role distribution section with 3 icon+number+label cards. Player name list with role icons. Large Start Game CTA button."

Screen 5 (Online) — Join the Battle

Two side-by-side panels: Create Room (with name input + button) and Join Room (with name + room code input + button). Quick game overview stepper at the bottom with 3 steps.
Figma prompt: "Mobile screen split into 2 vertical panels. Left panel: plus icon, 'Create Room' title, name input, teal Create button, bullet list of room details. Right panel: enter icon, 'Join Room' title, name + room code inputs, Join button, bullet list. Below both panels: horizontal 3-step overview with numbered circles."

Screen 6 — Game Lobby

Room code displayed prominently in a large teal bar with a Copy button. Game status bar (orange, showing waiting state). Player count and phase info. Game theme selector dropdown below.
Figma prompt: "Lobby screen. Header with Leave Room link and Start Game button. Large teal room code display bar with Copy Code button. Game Status section with orange status bar, player count stats, and phase label. Game Theme section with dropdown selector."

Screen 7 — Player List in Lobby

Grid of player slots (2 columns), filled slots show avatar + name, empty slots show ghost icon + 'Waiting for player…'. Quick Rules section below with 3 step cards.
Figma prompt: "Player grid with 2-column layout. Filled slots: colored avatar circle + name + player number. Empty slots: greyed ghost icon + waiting label. Quick Rules section below with 3 colored step cards (blue, green, purple) each with step number, title, description."

Screen 8 — Choose Your Role Card

Title and subtitle at top. Progress tracker showing how many players have chosen. 2x2 grid of mystery role cards, each numbered with a stethoscope icon and 'Click to reveal' link.
Figma prompt: "Role selection screen. Centered stethoscope logo, bold title, subtitle. Progress status row. 2x2 card grid — each card has card number at top, icon centered, and teal 'Click to reveal' text. Cards have light border."

Screen 9 — Role Revealed State

After clicking, the selected card expands to show the disease name ('YOUR DISEASE' label, disease name in bold, confidentiality reminder). Other cards show 'Taken by [name]' in red. Player Selection Status grid at bottom shows all players and their selected card numbers.
Figma prompt: "Role card revealed state. Selected card highlighted with green checkmark badge, expanded inner panel showing disease label, disease name bold, emoji reminder text. Other cards show red lock icon + Taken label. Below: Player Status grid (2 columns) with avatar, name, and card number per player."

Screen 10 — Discussion Phase

Active Players list at top. Discussion Phase banner with clock icon and description. Text area for submitting a diagnosis clue with a Submit button. Game Chat section below showing player messages with timestamps.
Figma prompt: "Game screen. Top: active player list with avatars and host badge. Phase banner (clock icon, phase name, description). Text input area full width with Submit button. Chat section with player-colored name, message text, and timestamp per row."

Screen 11 — Voting Phase

Voting Status header with vote count. Active Players list where each non-host player has a Vote button. Voting Phase explanation banner. Game Chat visible below.
Figma prompt: "Voting screen. Voting status header with fraction counter. Player list rows — each row has colored avatar, name, and right-aligned Vote button (except host). Phase explanation banner. Game chat section below."

Screen 12 — Final Round / You're a Finalist

Trophy icon with 'You're a Finalist!' message. Note that eliminated players are voting. Vote progress counter (0/1).
Figma prompt: "Finalist waiting screen. Large orange trophy icon, bold title, subtitle explaining eliminated players are voting. Light blue progress box with fraction and label centered."

Screen 13 — Final Round Submissions

Numbered finalist cards (1, 2, 3), each showing the player's name, Treatment Plan, and Medical Innovation submissions. Game Chat visible below.
Figma prompt: "Final submissions list. Each card: numbered badge (teal circle), player name, Finalist label, two labeled text sections (Treatment Plan with stethoscope icon, Medical Innovation with trophy icon), each showing the submitted text. Game chat at bottom."

Screen 14 — Game Over / Leaderboard

'Game Over!' splash with winner team announcement (e.g., Civilian Menang!). Reveals the secret words used (Civilian word vs Undercover word). Leaderboard below with rank, avatar, name, role badge, point breakdown, and total score. Play Again and Home buttons.
Figma prompt: "Game over screen. Top: blue splash panel with trophy, Game Over title, winner team badge, 'all undercovers eliminated' text, two-column word reveal (Civilian word / Undercover word). Below: leaderboard with rank number, colored avatar, player name, role chip, point breakdown text, right-aligned score. Gold highlight for 1st place. Two bottom buttons: Play Again and Home."

Special Stage / Quiz Flow (handwritten sketch)

A branching flow where players enter a quiz, answer A/B/C/D questions, get rewards if correct (bonus points, immunity for 1 round, or ask a friend), and wrong answers pass to the next player.
Figma prompt: "Quiz overlay screen. Question text at top. 4 answer option buttons (A/B/C/D). On correct answer: reward selection screen with 3 options (ask a friend, bonus points, immunity 1 round) shown as tappable bubble cards. Player selector for who receives the reward."